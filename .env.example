# Where the BugBoard frontend is served. UI tests navigate here.
BASE_URL=http://localhost:5173

# Where the BugBoard API is served. API tests call this directly.
API_URL=http://localhost:4000

# Credentials from the app's seed fixture.
ADMIN_EMAIL=admin@bugboard.dev
ADMIN_PASSWORD=Password123!
MEMBER_EMAIL=dev@bugboard.dev
MEMBER_PASSWORD=Password123!
